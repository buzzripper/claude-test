# SQL Scripts

The database schema is automatically created by Entity Framework migrations.

Run migrations:
```bash
cd src/Auth/Auth.Host
dotnet ef database update
```

See the previous solution package for complete SQL reference scripts.
